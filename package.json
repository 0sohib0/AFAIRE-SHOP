{
  "name": "sohib-hamza-static-store",
  "version": "1.0.0",
  "description": "Static site for order collection using Supabase direct",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.4"
  }
}
