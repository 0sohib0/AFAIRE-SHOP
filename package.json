{
  "name": "acme-store-dz-backend",
  "version": "1.0.0",
  "description": "Serverless backend for order collection using Supabase",
  "main": "api/submit-order.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.4"
  }
}
